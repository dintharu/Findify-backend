spring:
  datasource:
    url: jdbc:mysql://localhost:3306/findify_user_db?createDatabaseIfNotExist=true
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080

# ... existing configurations ...

application:
  security:
    jwt:
      secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970 # Base64 encoded, 256-bit key (generate a strong one)
      expiration: 86400000 # 24 hours in milliseconds (86400000 = 24 * 60 * 60 * 1000)